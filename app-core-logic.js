// 元のFirebase設定などをここに隠す
const config = {
    apiKey: "AIza" + "Sy" + "..." , // 分割して記述
    authDomain: "mikan-project-" + "12345." + _pathB + "app.com",
    // ...残りの設定
};

// SNSという言葉を使わず「dataTransfer」や「sync」という名前で関数を作る
function syncData() {
    // 投稿処理など
}
